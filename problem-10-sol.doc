Line 13: int choice = 1
Error: Semicolon missing. Every statement execution line in C must end with a semicolon delimiter.
Correction: Add a semicolon at the end: int choice = 1;

Line 15: int marks = "90";
Error: Incompatible type assignment string to integer mismatch. Assigning a string constant directly to an int data type is invalid.
Correction: Change it to a normal integer value: int marks = 90;

Line 24: result = add(a);
Error: Too few arguments passed to function. The definition requires two arguments, but only one parameter was supplied.
Correction: Supply the missing argument: result = add(a, b);

Line 28: if(a = b)
Error: Assignment operator used inside conditional logic instead of standard equality comparison token.
Correction: Use the comparison operator: if(a == b)

Line 33: printf("%d\n", score);
Error: Undeclared identifier variable error. 'score' is referenced here but has never been declared within the scope.
Correction: Change it to the valid declared variable name: printf("%d\n", marks);

Line 37: arr[10] = 100;
Error: Array index boundary breach. The array size is constrained to 5, which makes access to index 10 completely out of bounds.
Correction: Use a valid bounds offset: arr[4] = 100;

Line 39: printf("%d\n", arr[-1]);
Error: Negative index array constraint violation. Accessing an array with a negative subscript index leads to undefined stack corruption behavior.
Correction: Change the reference index to a valid value: printf("%d\n", arr[0]);

Line 43: strcpy(name, "ProgrammingLanguage");
Error: String destination buffer overflow space violation. The target string buffer has only 10 bytes allocated, but the text requires 20 bytes.
Correction: Expand array allocation limit dimensions: char name[20];

Line 47: city = "Delhi";
Error: Array type lvalue assignment error restriction. In C, you cannot directly assign a string literal value to a defined array identifier name.
Correction: Utilize the string copying utility: strcpy(city, "Delhi");

Line 55: if(user == "admin")
Error: String pointer direct comparison logical defect. Comparing string literal targets using equality tokens compares addresses instead of base string contents.
Correction: Employ the standard library matching utility: if(strcmp(user, "admin") == 0)

Line 63: printf("%d\n", x/y);
Error: Division by zero critical exception error runtime crash. The variable 'y' holds a zero value, causing immediate system exception interruption.
Correction: Initialize 'y' to a non-zero value: int y = 1;

Line 67: *ptr1 = 500;
Error: Null pointer target content modification memory fault crash. Dereferencing a pointer that explicitly targets NULL triggers immediate stack exceptions.
Correction: Direct pointer to actual variable storage space before executing modifications: ptr1 = &x;

Line 71: printf("%d\n", *ptr2);
Error: Uninitialized pointer variable reference defect. Accessing data by dereferencing a completely unallocated wild reference yields random stack crash actions.
Correction: Initialize pointer properly before execution.

Line 81: printf("%d\n", *ptr3);
Error: Use after free allocation lifetime tracking error. The memory chunk referenced was explicitly released back to system management layers on the previous line.
Correction: Shift the diagnostic output command block ahead of the free tracking release routine.

Line 83: free(ptr3);
Error: Double free memory space corruption runtime system failure. Invoking the deallocation resource release command twice sequentially on an unchanged pointer breaks allocation tracking mechanics.
Correction: Erase this redundant secondary memory deallocation macro block line completely.

Line 91: ptr4[i] = i;
Error: Dynamic heap allocation buffer space overflow write defect. Only 5 bytes were reserved via malloc, yet loop cycles overwrite 10 stack units.
Correction: Resize baseline block generation statements: ptr4 = malloc(10 * sizeof(int));

Line 99: ptr5 = malloc(sizeof(int)); ptr5 = malloc(sizeof(int));
Error: Memory allocation leak via reference overwriting. Initial block targets become completely untrackable when the secondary allocation statement is applied directly on the same identifier.
Correction: Embed appropriate release calls prior to shifting assignment states: free(ptr5);

Line 103: avg = average(70,80);
Error: Insufficient function parameter data compilation block. The matching method footprint requires exactly three arguments, but only two values are supplied.
Correction: Fill in the remaining parameter criteria state: avg = average(70, 80, 90);

Line 115: isPrime = 1;
Error: Logical evaluation defect in prime routine tracker. The loop flag remains unconditionally set to one, incorrectly concluding that composite factors represent prime states.
Correction: Swap state variables on matching criteria break actions: isPrime = 0;

Line 127: displayReport();
Error: Undefined reference function linkage failure. The invoked routine signature has placeholder declarations but completely lacks functional backing blocks.
Correction: Provide the missing implementation structure body down below.

Line 129: calculateSalary();
Error: Completely missing declaration signature error tracking. The routine call lacks explicit prototyping profiles and code execution body statements.
Correction: Declare and code the respective structural functions.

Line 143: case 1
Error: Switch block grammar syntax execution block defect. Colons are required to properly isolate case literal targets from statement blocks.
Correction: Insert the missing colon character target: case 1:

Line 147: case 2: printf("Two\n");
Error: Missing break keyword fall-through block flaw. Omitting execution break tracking flows forces control directly downward into secondary fallbacks.
Correction: Append structural exit tracking statements: break;

Line 153: char grade = 'AB';
Error: Multi-character byte sequence character constant restriction error. Character variables can store only individual scalar literal bytes.
Correction: Switch over to standard individual bytes: char grade = 'A';

Line 160: scores[3]=40;
Error: Local integer array overflow index error. Size allocation parameters restrict targets from index 0 to 2, causing index 3 to overrun allocations.
Correction: Increase the static sequence dimensions: int scores[4];

Line 164: for(int i=0;i<=3;i++)
Error: Loop processing index array out of bounds boundary overrun. Accumulation calculations scale up through index 3 which hits unallocated target elements.
Correction: Bound loop actions within safe array limitations: i < 3;

Line 175: area = 2 * 3.14 * radius * radius;
Error: Geometric mathematical formula logic blunder. The computation formula typed here represents a wrong mathematical structure for circle tracking bounds.
Correction: Adjust computation formulas to proper standards: area = 3.14 * radius * radius;

Line 179: while(choice > 0);
Error: Semicolon termination loop lock structure failure. Appending loop-ending tokens directly past evaluation criteria blocks seals operations inside infinite empty cycles.
Correction: Remove the trailing loop termination semicolon character.

Line 184: continue;
Error: Jump statement scope compilation restriction. Continue operational control markers are only valid inside loops, rendering them completely illegal within raw body roots.
Correction: Strip this statement out of the root level code environment.

Line 196: return (a+b+c)/3;
Error: Integer division precision loss calculation defect. Dividing standard integer numbers drops remainder fractional elements before saving into precision floats.
Correction: Enforce explicit fractional scaling formats: return (a + b + c) / 3.0;